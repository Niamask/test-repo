#include <stdio.h>
/**
 *main - prints the 9 times table, starting with 0.
 * @b : b integer
 *
 * Return: nothing
 */

int main(void)
{
        char c;

        for (c = 'z'; c >= 'a'; c--)
        {
                putchar(c);
        }
        putchar('\n');
        return(0);
}

