#include <ctype.h>
#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a : Two character to add
 * @b : Two character to add
 * Return: the result of add.
 */

int add(int a, int b)
{
	return (a + b);
}
